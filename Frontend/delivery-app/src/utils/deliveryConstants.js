export const DELIVERY_STATUS = { NEW: 'new', ACCEPTED: 'accepted', PICKED_UP: 'picked_up', IN_TRANSIT: 'in_transit', DELIVERED: 'delivered', CANCELLED: 'cancelled' }; export const STATUS_META = { [DELIVERY_STATUS.NEW]: { label: 'New request', color: 'warning' }, [DELIVERY_STATUS.ACCEPTED]: { label: 'Accepted', color: 'info' }, [DELIVERY_STATUS.PICKED_UP]: { label: 'Picked up', color: 'secondary' }, [DELIVERY_STATUS.IN_TRANSIT]: { label: 'In transit', color: 'primary' }, [DELIVERY_STATUS.DELIVERED]: { label: 'Delivered', color: 'success' }, [DELIVERY_STATUS.CANCELLED]: { label: 'Cancelled', color: 'error' } }; export const DELIVERY_STAGES = [ DELIVERY_STATUS.ACCEPTED, DELIVERY_STATUS.PICKED_UP, DELIVERY_STATUS.IN_TRANSIT, DELIVERY_STATUS.DELIVERED ]; export const activeStatuses = [ DELIVERY_STATUS.ACCEPTED, DELIVERY_STATUS.PICKED_UP, DELIVERY_STATUS.IN_TRANSIT ]; export const getActiveDeliveries = (list = []) => list.filter((delivery) => activeStatuses.includes(delivery.status)); export const getNewDeliveries = (list = []) => list.filter((delivery) => delivery.status === DELIVERY_STATUS.NEW); export const nextStopOf = (delivery) => delivery.status === DELIVERY_STATUS.ACCEPTED || delivery.status === DELIVERY_STATUS.NEW ? { kind: 'pickup', ...delivery.pickup } : { kind: 'drop', ...delivery.drop };
